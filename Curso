Para encontrar archivos ocultos en poweshell:
Get-ChildItem -h

COMANDOS GIT

Para encontrar archivo gitconfig:
git config --list --show-origin  

Para editar archivo gitconfig:
git config --global --edit

Para conocer estado del git:
git status

Para añadri ficheros
git add "nombreFichero"

Para subirlos definitivamente:
git commit -m "comentario"

Para saber los commit que hemos hecho:
git log

Para recuperar archivo de antes:
git chekout "nombreArchivo"

Para recuperar una rama
git chekout "id de la rama" 

Para encabezar una de las ramas
git chekout HEAD

Para verlo en formato rama:
git log --graph 
git log --graph --pretty=oneline
git log --graph --decorate --all --oneline

Para ver los cambios con respecto a la anterior version
git diff


